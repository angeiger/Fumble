<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        A deliberately thin XML theme: everything visual comes from Compose.
        This exists only to paint the right background during window start-up so
        there is no flash before the first frame, and to keep system bar icons dark.
    -->
    <style name="Theme.Fumble" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/fumble_canvas</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
